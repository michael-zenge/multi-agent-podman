# @generated
# Reviewed and Edited by the author of the codebase.
#
# This code is part of the Multi-Agent Podman project.
#
# podman-compose.yml - Podman Compose file for running multiple chatbot services

version: "3.9"

services:
  bot1:
    build:
      context: .. # Tells Podman to look at the workspace root
      dockerfile: bot_base/Dockerfile # Path to the Dockerfile from the root
    image: localhost/chat-bot:latest
    container_name: bot1
    ports:
      - "8001:8000"
    environment:
      BOT_NAME: "bot-1"
      MODEL_NAME: "llama3.2:latest"
      OLLAMA_HOST: "http://host.containers.internal:11434"

  bot2:
    build:
      context: .. # Tells Podman to look at the workspace root
      dockerfile: bot_base/Dockerfile # Path to the Dockerfile from the root
    image: localhost/chat-bot:latest
    container_name: bot2
    ports:
      - "8002:8000"
    environment:
      BOT_NAME: "bot-2"
      MODEL_NAME: "llama3.2:latest"
      OLLAMA_HOST: "http://host.containers.internal:11434"